/**
 * Public API for `shared/test` — test-only utilities consumed via `vi.mock`
 * deep imports and, since steiger 0.6 required a barrel here, via this file
 * for anything else.
 */
export * from './radix.js';
